# MPO

## Local configuration

Copy `MPO_Web_Prj/appsettings.example.json` to `MPO_Web_Prj/appsettings.json` and set the PostgreSQL connection string for your environment. The local appsettings file, build output, and development secrets are ignored by Git.
